@model IEnumerable<LibraryRepository.Models.Loan>

@{
    ViewData["Title"] = "Index";
}

<h1>List of all loans</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Member
            </th>
            <th>
                Rented Item
            </th>
            <th>
                Rent Date
            </th>
            <th>
                Return Date
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.MemberRenting.Name)
                </td>
                @if (item.BookRented != null)
                {
                    <td>
                        @Html.DisplayFor(modelItem => item.BookRented.Title)
                    </td>
                }
                else
                {
                    <td>
                        @Html.DisplayFor(modelItem => item.MovieRented.Title)
                    </td>
                }
                <td>
                    @Html.DisplayFor(modelItem => item.StartDate)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.EndDate)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                    @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.Id })
                </td>
            </tr>
        }
    </tbody>
</table>
